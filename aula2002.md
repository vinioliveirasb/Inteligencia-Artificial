# 🤖 Aula 2 — Fundamentos de IA, Métodos de Busca e Tratamento de Restrições

---

# 1️⃣ O que é Inteligência Artificial?

Inteligência Artificial é a área da Computação que desenvolve metodologias, técnicas e algoritmos para resolver problemas complexos que normalmente exigiriam especialista.

A IA resolve problemas que:

- Possuem muitas restrições
- Não têm estado final totalmente conhecido
- Precisam descobrir o passo-a-passo da solução

Exemplos:
- Resolver um quebra-cabeça 3x3
- Reconhecer padrões em imagens médicas
- Encontrar o melhor caminho entre dois pontos

---

# 2️⃣ Sistema de Comportamento Inteligente

Um sistema inteligente é composto por:

## 🔹 2.1 Base de Conhecimento
- Fatos, regras ou amostras.
- Funciona como memória do sistema.

Exemplo:
Banco de imagens de pulmões saudáveis e lesionados para treinar reconhecimento.

---

## 🔹 2.2 Raciocínio Automatizado
- **Dedutivo** → aplica regras para chegar a conclusões.
- **Indutivo** → aprende padrões a partir de exemplos.

---

## 🔹 2.3 Métodos de Busca (Motor de Resolução)

Responsável por explorar possíveis soluções.

### 📌 Busca Cega (Força Bruta)
- Busca em Amplitude (BFS)
- Busca em Profundidade (DFS)

Explora estados até encontrar a solução.

### 📌 Métodos Heurísticos
- Guloso
- Subida de Encosta
- A*
- Algoritmo Genético

Usam estratégias para reduzir o número de estados explorados.

---

## 🔹 2.4 Aprendizado de Máquina

- Baseado na repetição de amostras.
- Quanto mais exemplos, melhor o sistema aprende.
- Ajusta desempenho ao longo do tempo.

---

# 3️⃣ Modelagem de Problemas em IA

Para resolver um problema é necessário definir:

- **Estados**
  - Inicial
  - Final (objetivo)

- **Regras de Transição**
  - Como um estado pode mudar para outro

- **Restrições**
  - O que não pode acontecer

- **Lista de Visitados**
  - Evita repetir estados

- **Função Objetivo**
  - Define quando a solução foi alcançada

---

# 4️⃣ Exemplo Prático — Quebra-cabeça 3x3

Problema:

Grade 3x3 com números de 1 a 8 e um espaço vazio.

Estado exemplo:

[1 2 3]  
[4 5 6]  
[7 _ 8]

O vazio está na **linha 3, coluna 2**.

---

# 5️⃣ Tratamento de Restrições

O vazio:

- ❌ Não pode sair da matriz
- ❌ Não pode mover para baixo (já está na última linha)
- ✅ Pode mover para cima
- ✅ Pode mover esquerda
- ✅ Pode mover direita

O movimento depende da posição atual.

---

## 💻 Exemplo de Validação (Python)

```python
def movimento_valido(linha, coluna, direcao):
    if direcao == "cima" and linha > 0:
        return True
    if direcao == "baixo" and linha < 2:
        return True
    if direcao == "esquerda" and coluna > 0:
        return True
    if direcao == "direita" and coluna < 2:
        return True
    return False
```

⚠️ A validação deve buscar o erro.
Se a condição não for satisfeita, o movimento é inválido.

---

## 💻 Gerando Novo Estado

```python
def mover(tabuleiro, linha, coluna, direcao):
    if not movimento_valido(linha, coluna, direcao):
        return None  # descarta estado inválido
    
    novo = [linha[:] for linha in tabuleiro]

    if direcao == "cima":
        novo[linha][coluna], novo[linha-1][coluna] = novo[linha-1][coluna], novo[linha][coluna]

    if direcao == "baixo":
        novo[linha][coluna], novo[linha+1][coluna] = novo[linha+1][coluna], novo[linha][coluna]

    if direcao == "esquerda":
        novo[linha][coluna], novo[linha][coluna-1] = novo[linha][coluna-1], novo[linha][coluna]

    if direcao == "direita":
        novo[linha][coluna], novo[linha][coluna+1] = novo[linha][coluna+1], novo[linha][coluna]

    return novo
```

Estados inválidos são descartados imediatamente.

---

## 💻 Exemplo Simplificado — Busca em Profundidade (DFS)

```python
def dfs(estado, objetivo, visitados):
    if estado == objetivo:
        return True

    visitados.append(estado)

    for movimento in ["cima", "baixo", "esquerda", "direita"]:
        novo_estado = gerar_estado(estado, movimento)

        if novo_estado and novo_estado not in visitados:
            if dfs(novo_estado, objetivo, visitados):
                return True

    return False
```

A busca explora estados até encontrar a solução.

---

# ⚠️ Observações Importantes

- No código, deve-se validar buscando o erro, não o acerto.
- Estados inválidos devem ser descartados imediatamente.
- Em força bruta, a IA testa várias possibilidades.
- Se houver solução, ela encontrará (desde que explore corretamente os estados).
- Métodos heurísticos reduzem o custo computacional.

---

# ✅ Estrutura Final de um Sistema Inteligente

Base de Conhecimento  
+ Raciocínio  
+ Método de Busca  
+ Restrições  
+ Aprendizado  
= Sistema de Comportamento Inteligente
